============
Installation
============

At the command line::

    $ easy_install django-mappings

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mappings
    $ pip install django-mappings
